on:
  pull_request:
  release:
    types: [published]

name: build-windows-release

jobs:
  build:
    name: Build x86_64
    runs-on: windows-2025
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.5'
          tools: composer

      - name: Set a binary name
        run: echo "BINARY_NAME=my-prompts-mcp-${{ github.event.release.tag_name }}-windows-x86_64.exe" >> $GITHUB_ENV
        shell: bash

      - name: Build binary
        env:
          BINARY_NAME: ${{ env.BINARY_NAME }}
        run: bash .github/workflows/build-windows-release.sh
        shell: bash

      - name: Upload binary to release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload "test-tag" build/${{ env.BINARY_NAME }} --clobber
        shell: bash
