# Code style

- Use PHP 8.5 features.
- Always use English in code: variable names, comments, error messages, strings.
- Always use fully qualified class/function names via `use` statements instead of inline global references (e.g. `use Closure;` instead of `\Closure`).
